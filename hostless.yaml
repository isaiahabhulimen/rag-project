build:
  system: docker

dockerfile_path: "./Dockerfile"

working_directory: "/"
build_context_directory: "/"

scaling:
  min_cpu: 0.1
  max_cpu: 1
  min_memory: 128
  max_memory: 1024
  min_replicas: 1
  max_replicas: 1

health_check:
  type: http
  http_path: "/health"
  interval_in_seconds: 30